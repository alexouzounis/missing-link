= TODO =
  * apache ant http task
    * support basic authentication
    * support digest authentication
    * support TLS/SSL via configuration (not JVM wide)
    * support GET/PUT/POST methods
    * support in-line PUT/POST entities or file based
    * support arbitrary headers
    * support OPTIONS/HEAD/TRACE http methods (useful for testing and security purposes)
  * JKS keystore manipulation
    * import private key
    * export private key
    * keystore format manipulation
  * annotation based xml library
    * support all possible XSD features through simple annotations
    * support, in a simple manner, marshaling and unmarshaling XML to objects and lists of objects
    * XSD -> POJO (with annotation) code generator
    * spring integration
    * jersey integration